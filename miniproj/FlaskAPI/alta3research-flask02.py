#!/usr/bin/python3
'''
Your script alta3research-requests02.py should demonstrate proficiency with 
the requests HTTP library. This script should:
    -Send a GET request to your Flask API; it should target the endpoint that 
    returns JSON.
    -Take the returned JSON and "normalize" it into a format that is easy for 
    users to understand.
'''

# import requests
# import pprint

# my_flask_home = "http://127.0.0.1:2224/home"
# my_flask_fact = "http://127.0.0.1:2224/fact"

# def main(): 
#     home_resp = requests.get(my_flask_home).json()
#     fact_resp = requests.get(my_flask_fact).json

#     # homejson = home_resp.json()
#     # factjson = fact_resp.json()

#     pprint.pprint(home_resp)
#     pprint.pprint(fact_resp)

# if __name__ == "__main__":
#     main()